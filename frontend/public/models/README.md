Drop GLTF/GLB refinery asset files here.

Each building ID should correspond to a model file named `<id>.glb` (e.g. `cdu.glb`, `vdu.glb`, etc.).
The scene will load these models automatically; if a file is missing the primitive fallback will be used.

Use the provided reference image as a guide when creating the models to replicate the exact layout and appearance.