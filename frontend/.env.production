# Baked into the client bundle at build time. No secrets here.
NEXT_PUBLIC_API_URL=https://refinery-api.astrikos.xyz:8443

# Server-side only: server components fetch the API over loopback, skipping
# TLS + Cloudflare. Must match the backend's pm2 PORT.
BACKEND_INTERNAL_URL=http://127.0.0.1:4307
