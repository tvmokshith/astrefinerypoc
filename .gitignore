# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Next.js build output
frontend/.next/
frontend/out/

# Environment variables
frontend/.env
frontend/.env.local
# frontend/.env.production is committed on purpose — it holds only the public API
# URL that gets baked into the build (no secrets). See DEPLOY.md.
backend/.env
backend/.env.local

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db
